// These are dummy functions, to allow the TKL Performer scripts to run.
// To get the real versions of these functions, go to nwnx.org

void StartTimer(object oObject, string sName);
string StopTimer(object oObject, string sName);
string QueryTimer(object oObject, string sName);
const string timeSpacer = "                                               ";
void StartTimer(object oObject, string sName){}
string StopTimer(object oObject, string sName){return "";}
string QueryTimer(object oObject, string sName){return "";}